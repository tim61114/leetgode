package main

import "math"

func maxProduct(nums []int){
    MAX = nums[0]
    cmax := nums[0]
    cmin := nums[0]
    for i := 1;i < len(nums);i++ {
        mmax = cmax*nums[i]
        mmin = cmax*nums[i]

    }
}
